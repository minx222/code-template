{
	"name": "low-code",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "cd packages/admin && pnpm dev",
		"lint:eslint": "eslint . --ext .vue,.ts,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore",
		"lint:prettier": "prettier --write  \"packages/**/src/**/*.{js,json,tsx,css,less,scss,vue,html,md}\"",
		"lefthook": "lefthook install",
		"changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
		"commit": "git status && git add -A && git-cz && git push"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@commitlint/cli": "^18.6.0",
		"@commitlint/config-conventional": "^18.6.0",
		"commitizen": "^4.3.0",
		"cz-git": "^1.9.4",
		"lefthook": "^1.7.11"
	},
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"config": {
		"commitizen": {
			"path": "node_modules/cz-git"
		}
	}
}
